#include <iostream>
#include <string>
using namespace std;

void pro() {
  string s;
  cin >> s;
  int acnt, bcnt;
  int len = s.size();
  if()
  for(int i=1 ; i<len ; i++) {
    if()
  }
}

int main() {
  cin.tie(NULL);
  ios_base::sync_with_stdio(false);
  int t;
  cin >> t;
  while(t--) pro();
}