#include <stdio.h>
#include <queue>
#define INF 1000000000
using namespace std;

queue<int> Q;
int a[5][5], md[5][5], n[5][5];
int dx[4]={0, 0, 1, -1}, dy[4]={1, -1, 0, 0};

void bfs()
{
    int xx, yy, tx, ty;

    for(i=1 ; i<=4 ; i++)
        for(j=1 ; j<=4 ; j++)
        {
            Q.push()
        }
}

int main()
{
    int i, j;

    for(i=1 ; i<=4 ; i++)
        for(j=1 ; j<=4 ; j++)
            scanf("%d", &a[i][j]), md[i][j] = nd[i][j] = INF;
}
