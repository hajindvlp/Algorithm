#include <stdio.h>
#include <algorithm>
using namespace std;

int n, a[1001];
int d[100000001];

int main()
{
    int i, j, val;

    scanf("%d", &n);
    for(i=1 ; i<=n ; i++)
        scanf("%d", &a[i]);
    sort(a+1, a+n+1);

    
    for(i=1 ; ; i++)
    {
    }

}
