#include <stdio.h>
#include <queue>
using namespace std;

queue<int> Q;
int n, m, visit[105][75], a[105][75];

int main() {
    int i, j;

    scanf("%d %d", &n, &m);
    for(i=1 ; i<=n ; i++) for(j=1 ; j<=m ; j++) scanf("%d", &a[i][j]);

    for(i=1 ; i<=n ; i++) {
        for(j=1 ; j<=m ; j++) {
            
        }
    }
}